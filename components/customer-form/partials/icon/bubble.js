// pop up bubble describing icon state
const Bubble = ({
    bubbleTitle
}) => {

    return (
        <div className='bubble'>
            {bubbleTitle}
        </div>
    );
}

export default Bubble;